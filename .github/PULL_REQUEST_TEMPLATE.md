## What this changes

## Why

## Checklist

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check .` passes
- [ ] New/changed rules cite their source (Finance Act / CBDT notification) in a docstring
- [ ] Non-obvious edge cases are documented in the module docstring, not just in tests
